<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VolumeControl" parent="Theme.Material3.DayNight">
        <item name="android:tint">@color/skyBlue</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorSurfaceContainer">@color/black</item>
        <item name="colorPrimary">@color/skyBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:windowBackground">@color/black</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Base.Theme.VolumeControl.Button">
        <item name="android:textColor">@color/black</item>
    </style>
    
    <style name="DefaultAlertDialogStyle" parent="Theme.Material3.Dark.Dialog.Alert">
        <item name="colorSurfaceContainerHigh">@color/black</item>
        <item name="colorPrimary">@color/skyBlue</item>
        <item name="scrimBackground">@color/skyBlue</item>
    </style>
</resources>